# Implementation Plan - Verify Issue #17 (User Deletion Safeguards)

## 1. 🔍 Analysis & Context
*   **Objective:** Verify that the logic preventing accidental user deletion (when removed from a team) is correctly implemented and covered by tests, allowing Issue #17 to be closed.
*   **Affected Files:** `api/NikoNiko.Services/TeamService.cs`, `api/NikoNiko.Api.IntegrationTests/TeamsControllerRemoveUserTests.cs`
*   **Key Dependencies:** `NikoNiko.Api.IntegrationTests`
*   **Risks/Unknowns:** The current implementation creates a default team for orphans instead of deleting them. This deviates slightly from the original issue text ("can be fully deleted") but effectively resolves the main "data loss" bug.

## 2. 📋 Checklist
- [x] Step 1: Verify Safeguard Logic via Tests

## 3. 📝 Step-by-Step Implementation Details

### 🚀 Execution Rules
1. **Interactive Flow**: Execute ONLY ONE step at a time.
2. **Atomic Updates**: Before and after each step, you MUST use `replace` to update the checklist in section 2 and the status in section 3.
3. **Status Vocabulary**: Use `[x]` (Done), `[>]` (In Progress), `[-]` (Skipped/N.A.), `[!]` (Failed/Blocked).
4. **User Confirmation**: After updating the file, stop and wait for explicit user confirmation before proceeding to the next step.
5. **No Stealth Actions**: NEVER execute code or modify files without having first updated the plan to reflect that you are about to do so.

### Step 1: Verify Safeguard Logic via Tests [x]
*   **Goal:** Confirm that `RemoveUserFromTeam` does NOT delete the user entity, even if they are removed from their last team.
*   **Action:**
    *   Execute the integration tests that cover user removal scenarios.
    *   Command: `dotnet test api/NikoNiko.Api.IntegrationTests/NikoNiko.Api.IntegrationTests.csproj --filter "FullyQualifiedName~TeamsControllerRemoveUserTests"`
*   **Verification:**
    *   **Result:** Tests Passed (Total: 2, Failed: 0).
    *   `RemoveUser_ShouldDeleteMoods_And_RemoveLink`: Confirmed user data cleanup without account deletion.
    *   `RemoveUser_Orphan_ShouldCreateDefaultTeam`: Confirmed auto-creation of default team for orphans.

## 4. 🧪 Testing Strategy
*   **Integration Tests:** The `TeamsControllerRemoveUserTests` class specifically targets the scenarios described in Issue #17.
    *   `RemoveUser_ShouldDeleteMoods_And_RemoveLink`: Simulates removing a user who is in multiple teams.
    *   `RemoveUser_Orphan_ShouldCreateDefaultTeam`: Simulates removing a user who is in only one team.

## 5. ✅ Success Criteria
*   All tests in `TeamsControllerRemoveUserTests` pass.
*   Confirmation that `TeamService.cs` does not contain `_context.Users.Remove` calls within the `RemoveUserFromTeamAsync` method.
